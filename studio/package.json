{
  "name": "digitalsphereug-studio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "sanity dev",
    "build": "sanity build",
    "deploy": "sanity deploy",
    "seed:content": "sanity exec scripts/seed-content.mjs --with-user-token",
    "seed:refresh": "sanity exec scripts/seed-content.mjs --with-user-token -- --refresh",
    "seed:refresh:safe": "sanity exec scripts/seed-content.mjs --with-user-token -- --refresh --preserve-settings"
  },
  "dependencies": {
    "@sanity/vision": "^3.85.0",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "sanity": "^5.19.0",
    "styled-components": "^6.3.12"
  }
}
